const playToggle = document.getElementById("play-toggle");
const resetButton = document.getElementById("reset");
const themeToggles = Array.from(document.querySelectorAll("[data-theme-toggle]"));
const themeToggleLabels = Array.from(document.querySelectorAll("[data-theme-toggle-label]"));
const controlGroups = document.getElementById("control-groups");
const statsGrid = document.getElementById("stats-grid");
const sceneCanvas = document.getElementById("scene-canvas");
const chartCanvas = document.getElementById("chart-canvas");
const audioUpload = document.getElementById("audio-upload");
const audioStatus = document.getElementById("audio-status");
const filePicker = document.getElementById("file-picker");
const uploadedAudio = document.getElementById("uploaded-audio");
const uploadedAudioName = document.getElementById("uploaded-audio-name");
const removeAudioButton = document.getElementById("remove-audio");
const liveAudioToggle = document.getElementById("live-audio-toggle");
const liveAudioToggles = Array.from(document.querySelectorAll("[data-live-audio-toggle]"));
const showNormalLocusToggle = document.getElementById("show-normal-locus");
const circularTrackToggle = document.getElementById("circular-track");
const audioEffectPrankInput = document.getElementById("audio-effect-prank");
const audioEffectNormalInput = document.getElementById("audio-effect-normal");
const liveSourceSongInput = document.getElementById("live-source-song");
const liveAudioStatus = document.getElementById("live-audio-status");
const songProgressEl = document.getElementById("song-progress");
const songProgressFill = document.getElementById("song-progress-fill");
const songProgressCurrent = document.getElementById("song-progress-current");
const songProgressTotal = document.getElementById("song-progress-total");

const sceneContext = sceneCanvas.getContext("2d");
const chartContext = chartCanvas.getContext("2d");
